# snowflake-connector-pythonにキーペアJWTの生成・SQL実行を委譲する。
# ネイティブ拡張(cryptography, nanoarrow)を含むため、terraform apply時に
# Lambdaランタイム(python3.13, manylinux2014_x86_64)向けのプリビルド済みwheelを取得する。
# egress_ip_sync.tfのnull_resource.egress_ip_sync_build参照。
#
# boto3/botocore/s3transfer/jmespathはLambdaランタイムに標準同梱されているため、
# ビルドステップでインストール後に除外する(ここには含めない)。
snowflake-connector-python>=3.14,<5
